.col-md-6
  %h1 Workspace#1
  %ul.list-group
  %li.list-group-item
    %a{ href:"#{ @parent }" }..
  -@directories.each do |dir|
    %li.list-group-item
      %a{ href:"#{dir}" }= File.basename(dir)
  -@files.each do |file|
    %li.list-group-item
      = File.basename(file)
